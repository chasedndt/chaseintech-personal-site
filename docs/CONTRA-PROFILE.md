# Contra — profile pack

## Status

Live already:
- ✅ Account created — John Idowu
- ✅ One-liner: *I build AI agents and automations safe enough to deploy.* (56/60)
- ✅ Bio (385/400)
- ✅ Rate: **$50–$75/hr** (matches the $60 Upwork rate)
- ✅ Topics followed: AI Automation (2.1k), AI Engineer (1k), AI Agent Development (579)
- ✅ Social links ×6: Portfolio, LinkedIn, GitHub, YouTube, TikTok, ChaseOS
  (Instagram deliberately excluded)
- ✅ Plan: Free
- ✅ Location: Luton → **London, UK** (matches LinkedIn)
- ✅ Profile URL registered on chaseintech.com: `contra.com/chase_in_tech_3c1qpk30`
  (wired into `src/data/site.js` → shows in the "Also on" section of `/work-with-me`)

Outstanding — **needs you**, because Contra requires an image per work item and
this browser has no file-upload capability (no `<input type=file>` access from
script — genuinely can't be automated, not a shortcut I skipped):
- ⬜ 4 pieces of work (real UI, not logos — see below)
- ⬜ Profile cover image
- ⬜ Identity verification + wallet (this is the KYC step)

---

## The 4 work pieces — real UI screenshots, staged on your Desktop

Folder: `Desktop/contra-work/`

Contra rejected a logo-only submission ("please add at least one image"), which
turned out to be the right prompt — a real product screenshot proves more than
a mark. Pulled these from the actual ChaseOS Studio build rather than making
new ones:

### 1 — `1-chaser-agent.png` (case-study card — no live UI exists for this one)
```
Chaser Agent — an open-source MIT agent harness built for review. It separates
what a source actually said, what the model inferred, what remains uncertain,
and what actions are proposed, so a reviewer can verify output in seconds
instead of redoing the work. Ships with 28 tests across 10 suites and 5 golden
evaluation datasets.
```

### 2 — `2-chaseos.jpg` — Studio home dashboard
Live counts on screen: 94 pending approvals, 394-item review queue, 1667-node
graph, real agent-activity feed. This is the strongest single proof image —
approval gates aren't a claim here, they're a number on the dashboard.
```
ChaseOS Studio — the home dashboard. A decision router picks human judgement,
deterministic rules, ML or generative AI per step, and nothing executes without
clearing the approval queue you see here. Live at chaseos.ai.
```

### 3 — `3-toolshape-studio.png` — real editor UI
```
Toolshape Studio — a creative tool where an AI agent and a human work on the
same project without silent corruption. Every edit an agent can make is a typed,
revision-checked operation rather than a blind write. The same operation layer
drives the UI, so anything a person can do an agent can do.
```

### 4 — `4-toolshape-voice.png` — real desktop app UI
```
Toolshape Voice — a local-first Windows dictation and writing-intelligence app.
Audio never leaves the machine and no target application is written to until
verified safe: secure fields are refused before capture, guarded input is
revalidated with UI Automation readback. Packaged desktop build in daily use.
```

## Two extra images, in case you want a 5th/6th piece or a stronger cover

- `5-chaseos-graph.jpg` — the knowledge-graph view, mid-interaction, with a
  live "3 approvals waiting" toast. Visually the most striking of the set —
  good candidate for the **profile cover image**.
- `6-chaseos-terminal.jpg` — the Studio terminal panel, with **Chaser Agent
  listed in the runtime launcher itself**, labelled *"not an autonomous
  runtime yet — this is diagnostic only."* That line is the governance
  boundary stated inside the product UI, not in marketing copy — arguably
  your best single piece of proof if Contra ever supports a 5th slot.
- `7-chaseos-companion.jpg` — an agent's companion profile panel, showing
  "94 approvals waiting on you" and a trust score. Good backup if any of the
  four above don't fit well once cropped.

**Recommended cover image: `5-chaseos-graph.jpg`.** It's the only one of the
set that reads well as a wide banner crop rather than a 4:3 screenshot.

---

## Why these four (unchanged from before)

Contra's real audience is AI Automation, AI Engineer, AI Agent Development —
confirmed by the onboarding topic list. Chaser Agent and ChaseOS prove governed
autonomy; Toolshape Studio and Voice prove you ship finished, used products.

---

## Fix

Contra has your location as **Luton, UK**; LinkedIn says **London Area**. Change
to London for cross-platform consistency — you've confirmed this, just needs
doing.
